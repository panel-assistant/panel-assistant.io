# Workers Static Assets serves everything with max-age=0 and revalidation by default. Files under
# /_astro/ have content hashes in their names, so they never change: cache them for a year and skip
# revalidation, which lets fonts and styles load from cache on every page after the first.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
